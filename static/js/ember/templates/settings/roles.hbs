
<ul>
  {{#each role in availableRoles itemController='role'}}
    <li>
      {{#if userRole}}
        <button {{action 'remove' userRole}}>Remove {{role.model}}</button>
      {{else}}
        <button {{action 'add' role.model}}>Add {{role.model}}</button>
      {{/if}}
    </li>
  {{/each}}
</ul>


